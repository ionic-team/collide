{
  "name": "collide",
  "version": "0.0.4",
  "main": "collide.js",
  "scripts": {
    "build": "mkdir -p dist && browserify src/index.js -s collide > dist/collide.js"
  },
  "dependencies": {
    "events": "^1.0.1",
    "promiscuous": "^0.6.0",
    "raf": "^2.0.1",
    "feature": "^0.1.4",
    "node-extend": "^0.2.0"
  }
}
